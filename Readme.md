# Commands
- ```/setchannel``` - Sets the channel ID in the server config
- ```/set_id``` - Sets the gang ID in the server config
- ```/caps``` - Shows current cartels caps with percentage (updated every minute)
- ```/id``` - Shows the current set gang ID for the server